const cache = {
    blocks: {
        properties: new Map(),
        buffer: new Map()
    },
    items: {
        properties: new Map(),
        buffer: new Map()
    }
};

export default cache;